# L1 push_t — action output = action_chunk × action_dim (default 4×2 = 8)
task: push
action_dim: 2
action_chunk: 4
data_dir: data/synthetic_push
output_dir: runs/push
num_episodes: 120
seed: 0
epochs: 3
batch_size: 4
lr: 0.001
eval_episodes: 50
eval_seed: 100
dry_run_episodes: 30
device: null
vlm_backbone: clip
