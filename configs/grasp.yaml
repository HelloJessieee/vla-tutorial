# L2 grasp — (dx, dy, gripper); output_dim = 3
task: grasp
action_dim: 3
action_chunk: 1
data_dir: data/synthetic_grasp
output_dir: runs/grasp
num_episodes: 120
seed: 0
epochs: 3
batch_size: 4
lr: 0.001
eval_episodes: 50
eval_seed: 100
dry_run_episodes: 30
device: null
vlm_backbone: clip
